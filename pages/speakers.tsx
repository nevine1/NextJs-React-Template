import React, { ReactElement } from 'react'

interface Props {
    
}

export default function speakers({}: Props): ReactElement {
    return (
        <div>
           <h1 className="h1 is-text-h1 has-text-primary">Search Page</h1> 
        </div>
    )
}
