import React, { ReactElement, Fragment } from 'react'

interface Props {
    
}

export default function Layout({}: Props): ReactElement {
    return (
        <Fragment>
            <h1>hello layout</h1>
        </Fragment>
    )
}
