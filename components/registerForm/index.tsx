import React, { ReactElement , Fragment } from 'react'

interface Props {
    
}

export default function index({}: Props): ReactElement {
    return (
        <Fragment>
            <h1>Hello REact form register </h1>
        </Fragment>
    )
}
